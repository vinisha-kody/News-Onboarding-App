import 'package:flutter/material.dart';

class CommonDomainChoiceButton extends StatelessWidget {
  const CommonDomainChoiceButton({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
    );
  }
}
